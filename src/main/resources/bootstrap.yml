aws:
  secretsmanager:
    prefix: /config
    defaultContext: payments
    profileSeparator: .
    name: payments
    environment: dev
    failFast: true
    enabled: true

cloud:
  aws:
    region:
      auto: false
      static: eu-west-2
    stack:
      auto: false
