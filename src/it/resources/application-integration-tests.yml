### Integration tests configuration

spring:
  datasource:
    url: jdbc:postgresql://${postgres.url:localhost:9999}/caz-payments
#    url: jdbc:postgresql://${postgres.url:localhost:5432}/caz-payments
    hikari:
      maximum-pool-size: 10

aws:
  sqs:
    endpoint: http://${aws.sqs.url:localhost:4576}

services:
  gov-uk-pay:
    root-url: http://localhost:1080
    api-key: dummy
  sqs:
    message-group-id-payments: TEST_RECEIPT
    new-queue-name: testqueue.fifo
    new-queue-url: http://localhost:4576/queue/testqueue.fifo
    template-id: test-template-id